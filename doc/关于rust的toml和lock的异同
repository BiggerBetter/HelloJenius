Toml VS Lock
toml是“TOML”这个名字是“Tom's Obvious, Minimal Language（汤姆的浅显的、极简的语言）”的首字母略写词[3]。“Tom”指它的作者Tom Preston-Werner。

	•	Cargo.toml 是配置文件，定义了项目的「依赖需求」和「元信息」。
	•	Cargo.lock 是实现这些需求的具体方案，锁定了依赖的具体版本和来源。

类比：
	1.	Cargo.toml 是你的订单清单：
	•	你在订单里写上了「我要一个尺寸为 M 的 T 恤，蓝色」。
	•	类似地，Cargo.toml 只描述「我需要哪个库，版本范围是多少」。
	2.	Cargo.lock 是你的订单实际配送内容：
	•	供应商实际给你的是「一件蓝色 M 号的 T 恤，来自品牌 A，生产日期为 2023-10-01」。
	•	对应地，Cargo.lock 记录了具体获取到的依赖的「版本、来源、校验码等」。

	关键区别：
    	•	Cargo.toml 和 Cargo.lock 的独立性：
    	•	Cargo.toml 可以由用户随意修改，例如你改动依赖库的版本范围。
    	•	Cargo.lock 是由 Cargo 根据 Cargo.toml 自动生成和更新的。
    	•	动态 vs 静态：
    	•	Cargo.toml 允许灵活的版本描述，例如 "1.2" 可以包含所有的 1.2.x 版本。
    	•	Cargo.lock 会将实际用到的具体版本（比如 1.2.3）记录下来。
    	•	目标用途：
    	•	Cargo.toml 关注「配置」，是团队协作时需要提交到版本控制系统的。
    	•	Cargo.lock 确保「构建一致性」，对于应用程序项目也应该提交到版本控制系统，防止不同开发者或环境下载到不同版本的依赖。


    	什么时候会更新 Cargo.lock？
        	1.	添加/修改依赖：
        修改 Cargo.toml 后，运行 cargo build 时会更新 Cargo.lock。
        	2.	依赖的版本更新：
        如果依赖允许较大的版本范围，运行 cargo update 会强制更新依赖到最新的兼容版本，并同步到 Cargo.lock。